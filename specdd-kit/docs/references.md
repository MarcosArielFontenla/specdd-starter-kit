# References

## Spec-Driven Development

- [`github/spec-kit`](https://github.com/github/spec-kit) — the SDD methodology
  and template shapes (`spec.md`, `plan.md`, `tasks.md`, the
  constitution → specify → plan → tasks → implement loop) that this kit is
  directly inspired by. See `docs/specdd-methodology.md` in this kit for how
  `specdd-kit` relates to and differs from it.

## Model Context Protocol (MCP)

- [Model Context Protocol specification](https://modelcontextprotocol.io/) — the
  open protocol that lets agents connect to external tools/data sources via a
  standard interface. This is what `.vscode/mcp.json` (generated by the wizard's
  MCP step) configures.
- [`@modelcontextprotocol/server-github`](https://www.npmjs.com/package/@modelcontextprotocol/server-github) —
  GitHub MCP server (issues, PRs, repo content).
- [`@modelcontextprotocol/server-postgres`](https://www.npmjs.com/package/@modelcontextprotocol/server-postgres) —
  PostgreSQL MCP server.
- [`@upstash/context7-mcp`](https://www.npmjs.com/package/@upstash/context7-mcp) —
  context7 MCP server (up-to-date library/framework documentation lookup).
- [`@playwright/mcp`](https://www.npmjs.com/package/@playwright/mcp) — Playwright
  MCP server (browser automation/testing).
- `sonarqube-mcp-server` — SonarQube MCP server (code quality/security findings).
- `figma-developer-mcp` — Figma MCP server (design file access).
- See `.github/instructions/mcp-tools.instructions.md` in this kit for the usage
  guardrails every agent here follows when calling MCP tools, and
  `docs/faq.md` for setup/secrets questions.

## VS Code / GitHub Copilot

- [VS Code custom instructions documentation](https://code.visualstudio.com/docs/copilot/copilot-customization) —
  how `.github/copilot-instructions.md` and `.github/instructions/*.instructions.md`
  are discovered and applied.
- [VS Code MCP servers documentation](https://code.visualstudio.com/docs/copilot/chat/mcp-servers) —
  how `.vscode/mcp.json` is discovered and how input variables/secrets are
  handled.

## Within this kit

- `README.md` — what the kit generates and how to run the wizard.
- `SETUP.md` — ordered setup: prerequisites → context → first feature → MCP → team
  adoption.
- `docs/starter-guide.md` — 10-minute first-feature walkthrough.
- `docs/specdd-methodology.md` — SDD explained and how this kit differs from
  `github/spec-kit`.
- `docs/workflow.md` — the full loop and every `/specdd-*` prompt.
- `docs/greenfield-vs-brownfield.md` — applying SDD to new vs. existing code.
- `docs/faq.md` — secrets, MCP, agent choice, offline use.
