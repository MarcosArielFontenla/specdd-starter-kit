{
  "name": "specforge-wizard",
  "version": "1.0.0",
  "description": "SpecForge Wizard - collect role-specific inputs and generate a Copilot-ready spec scaffold",
  "type": "module",
  "exports": { "./wizard": "./src/components/Wizard.jsx" },
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "bundle-skills": "node scripts/bundle-skills.js",
    "build:capability-model": "npm run build -w @specdd/capability-model",
    "predev": "npm run build:capability-model && node scripts/bundle-skills.js",
    "prebuild": "npm run build:capability-model && node scripts/bundle-skills.js",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "pretest:unit": "npm run build:capability-model",
    "test:unit": "node --test",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "dependencies": {
    "@specdd/capability-model": "*",
    "@specdd/ui": "*",
    "@astrojs/react": "^6.0.5",
    "astro": "^7.3.1",
    "jszip": "^3.10.1",
    "lucide-react": "^0.400.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0"
  }
}
